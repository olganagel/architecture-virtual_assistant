services:
  rasa:
    image: rasa/rasa:3.8.0-full
    ports:
      - "5005:5005"
    volumes:
      - ./:/app
    command: ["run", "--enable-api", "--cors", "*"]
  
  action-server:
    image: rasa/rasa-sdk:3.8.0
    volumes:
      - ./actions:/app/actions
    ports:
      - "5055:5055"